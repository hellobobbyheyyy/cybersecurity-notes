# Networking Fundamentals

---

## IP Address (Internet Protocol)

An IP address is a logical identifier used to identify a device on a network during communication.

### Key Points
- IPv4 addresses consist of 4 octets (e.g., 192.168.1.1)
- Each octet ranges from 0 to 255
- IP addresses can be static (fixed) or dynamic (change over time)
- Two devices cannot use the same IP address on the same network at the same time

### Types of IP Addresses

Public IP:
- Used to identify a device on the internet
- Assigned by an Internet Service Provider (ISP)
- Must be globally unique

Private IP:
- Used within local networks (home, office, internal systems)
- Not accessible directly from the internet
- Common ranges include:
  - 192.168.x.x
  - 10.x.x.x
  - 172.16–31.x.x

---

## MAC Address (Media Access Control)

A MAC address is a physical identifier assigned to a network interface card (NIC).

### Key Points
- Consists of 12 hexadecimal characters (e.g., 00:1A:2B:3C:4D:5E)
- The first 6 characters represent the manufacturer (OUI)
- The last 6 characters uniquely identify the device
- Used for communication within a local network (Layer 2)

---

## MAC Spoofing

MAC spoofing is the act of changing a device’s MAC address to impersonate another device.

### Common Uses
- Bypassing network restrictions
- Impersonating trusted devices
- Privacy or anonymity
- Security testing (ethical hacking)

---

## Summary

- IP Address: Identifies a device on a network for communication (logical)
- MAC Address: Identifies the physical device on a network (hardware)
- MAC Spoofing: Technique used to impersonate another device by altering the MAC address
-------------------------------------------------------------------------------------------------------------------------------
## LAN (Local Area Network)
- A Local Area Network connects devices in a small area (home, school, office)
- Devices can share files and internet

## Star Topology
- All devices connect to a central device (switch or hub)

Advantages:
- Reliable
- Easy to add devices
- One device failing does not affect others

Disadvantages:
- More expensive
- If central device fails, network goes down

## Bus Topology
- All devices share one main cable (backbone)

Advantages:
- Cheap
- Easy to set up

Disadvantages:
- Slower with more traffic
- Hard to troubleshoot
- If cable breaks, entire network fails

## Ring Topology
- Devices are connected in a loop

Advantages:
- Simple structure

Disadvantages:
- If one device fails, everything stops
- Slower data transfer

## Switch
- Connects devices in a network
- Sends data only to the correct device

## Router
- Connects different networks
- Sends data between networks

## My Understanding
- LAN = small network
- Star = central device
- Bus = one cable
- Ring = loop
- Switch = directs traffic
- Router = connects networks




-------------------------------------------------


# ARP (Address Resolution Protocol)

ARP is used to link an IP address to a MAC address on a network.

Each device keeps a table called an ARP cache.
This stores IP and MAC address mappings of other devices.

## How ARP works

When a device wants to communicate with another device:
- It first checks its ARP cache
- If the MAC address is not found, it sends a broadcast

## ARP Request
A message sent to all devices on the network:
"Who has this IP address?"

## ARP Reply
The device with that IP responds:
"I have that IP, here is my MAC address"

## What happens next
- The sender stores the mapping in its ARP cache
- Future communication is faster

## Key Points
- Works only on a local network (LAN)
- Uses broadcasting
- Helps devices find each other
--------------------------------------------------------------------

# DHCP (Dynamic Host Configuration Protocol)

DHCP automatically assigns IP addresses to devices on a network.

## DHCP Process (DORA)

## Discover
The device asks:
"Is there a DHCP server?"

## Offer
The server responds with an available IP address.

## Request
The device asks to use that IP address.

## Acknowledge (ACK)
The server confirms and allows the device to use the IP for a limited time (lease).

## Key Points
- IP addresses are temporary (leased)
- No manual setup needed
- Common in home and business networks

Network address- identifies the start of the network and used to identify a networks existence
Host address - ip address here is used to identify a device on the subnet

default gateway - is a special address assigned to a device on the network that is capable of sending info to another network


